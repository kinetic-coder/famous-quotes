import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Famous Quotes - Inspirational Quote Collection",
  description: "Discover, add, edit, and manage your favorite inspirational quotes. Build your personal collection of famous quotes from great minds throughout history.",
  keywords: "quotes, famous quotes, inspirational quotes, quote collection, wisdom, motivation",
  authors: [{ name: "Famous Quotes App" }],
};

export const viewport = {
  width: "device-width",
  initialScale: 1,
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
